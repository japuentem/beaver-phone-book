import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'beaver-phone-book',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
